class Solution(object):
    def maxSubArray(self, nums):
        max_total = float('-inf')
        total = 0
        
        for i in nums:
            total += i
            
            if total > max_total:
                max_total = total
            
            if total < 0:
                total = 0
        
        return max_total
